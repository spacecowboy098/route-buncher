# Secrets — passed at runtime via docker-compose env_file
.env

# Git
.git
.gitignore

# Python cache
__pycache__
*.pyc
*.pyo
*.pyd

# Virtual environments
.venv
venv
env

# Distance cache — mounted as a volume so it persists between restarts
distance_cache.json

# Editor / OS
.DS_Store
*.swp
